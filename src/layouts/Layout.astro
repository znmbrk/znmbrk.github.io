---
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props as Props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Zain Mobarik - Personal Website" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans leading-relaxed">
    <div class="min-h-screen flex flex-col">
      <nav class="w-full">
        <div class="px-6 py-8">
          <a href="/" class="text-gray-800 hover:text-gray-900 transition-colors">
            <span class="text-xl font-medium tracking-tight">Zain Mobarik</span>
          </a>
        </div>
      </nav>
      <div class="max-w-6xl mx-auto px-6 pb-8 flex-1">

        <main class="max-w-5xl">
          <slot />
        </main>
      </div>

      <footer class="border-t border-gray-200 bg-white">
        <div class="max-w-6xl mx-auto px-6 py-8">
          <p class="text-sm text-gray-500 text-center">
            © say hi.
          </p>
        </div>
      </footer>
    </div>
  </body>
</html>