# Strava API credentials — used at BUILD TIME only (see src/lib/strava.js).
# Copy this file to `.env` and fill in your values for local dev.
# In CI, these are provided via GitHub Actions Secrets (see .github/workflows/deploy.yml).
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=
